# Copy this file to .env locally for testing, or — better for a hackathon —
# just add ANTHROPIC_API_KEY directly in your Vercel project's
# Settings > Environment Variables. Never commit a real key to the repo.

ANTHROPIC_API_KEY=your_key_here
